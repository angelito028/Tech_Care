import 'package:flutter/material.dart';

class DispositivosRed extends StatelessWidget {
  const DispositivosRed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("PANTALLA DE DISPOSITIVOS DE RED")),
    );
  }
}
