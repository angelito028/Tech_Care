import 'package:flutter/material.dart';

class DispositivosES extends StatelessWidget {
  const DispositivosES({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Bienvenidos a Dispositivos Entrada Salida")),
    );
  }
}
